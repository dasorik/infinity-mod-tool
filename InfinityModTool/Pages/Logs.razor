@page "/Logs"

<h1>Logs</h1>

<hr />

<div class="log-textarea">
@foreach(var log in InfinityModTool.Utilities.Logging.GetLogs())
{
    <span class="@GetLogSeverityClass(log.severity)">[@log.date.ToString("dd/MM/yyyy hh:sstt")] - @log.message</span>
}
</div>

@code {

    public string GetLogSeverityClass(InfinityModTool.Utilities.Logging.LogSeverity severity)
    {
        switch (severity)
        {
            case InfinityModTool.Utilities.Logging.LogSeverity.Warning:
                return "warning";
            case InfinityModTool.Utilities.Logging.LogSeverity.Error:
                return "error";
        }

        return string.Empty;
    }

}